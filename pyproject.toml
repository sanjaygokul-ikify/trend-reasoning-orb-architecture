[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.builders:ResolveBuilder"

[tool.poetry]
name = "reasoning-orb-architecture"
version = "0.1.0"
authors = ["Your Name <email@example.com>"]